import { Matcher } from '../Matcher';
export declare function mount<Context extends object, Contexts extends {
    [P in Patterns]: object;
} = any, Patterns extends string = string>(paths: {
    [P in Patterns]: Matcher<Context extends Contexts[P] ? Contexts[P] : {}>;
}): Matcher<Context>;
